#include <iostream>

using namespace std;

enum class Color { red, blue, green };
enum class Traffic_light { green, yellow, red };

int main()
{
  Traffic_light tl = Traffic_light::red;
  Color c = Color::red;

  // wish I could print these values but idk enough yet lol
  return 0;
}
